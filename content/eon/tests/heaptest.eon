#include "content/eon/libs/libload.eon"
#include "content/eon/libs/sys.eon"

fn reada(addr) {
    asm "push 128";
    asm "sys 15";
    asm "ret";
}

fn main() {
    setupLibLoad();
    loadLib("/libs/heap.ell");

    var a = _HeapAlloc(128);

    print(a);
    print(reada(a));

    return 0;
}

