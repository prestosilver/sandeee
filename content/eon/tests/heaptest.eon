#include "content/eon/libs/heap.eon"

value reada(value addr) {
    asm "push 128";
    asm "sys 15";
    asm "ret";
}

value main() {
    setupHeap();
    value a = alloc(128);

    print(a);
    print(reada(a));

    return 0;
}
