#include "content/eon/libs/libload.eon"
#include "content/eon/libs/sys.eon"

value reada(value addr) {
    asm "push 128";
    asm "sys 15";
    asm "ret";
}

value main() {
    setupLibLoad();
    loadLib("/libs/heap.ell");

    value a = "HeapAlloc"(128);

    print(a);
    print(reada(a));

    return 0;
}

