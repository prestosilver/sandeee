#include "content/eon/libs/libload.eon"
#include "content/eon/libs/table.eon"
#include "content/eon/libs/sys.eon"

value main() {
    // load libs
    setupLibLoad();
    loadLib("/libs/string.ell");

    value table = tableCreate(2, 3);

    tablePut(table, "lo", "lol");

    print(tableGet(table, "lo"));

    asm "ret";
}
