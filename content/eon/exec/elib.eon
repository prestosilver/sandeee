#include "/libs/inc/libload.eon"
#include "/libs/inc/sys.eon"

fn main() {
    setupLibLoad();
    loadLib("/libs/eon.ell");

    var fileName = getArg(1);
    var outName = "out.ell";

    if (getArg(2) != "") {
       outName = getArg(2);
    }

    _eonFile(fileName, outName, 1);
    
    return 0;
}
