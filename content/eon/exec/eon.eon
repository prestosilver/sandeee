fn createToken(kind, val) {
    var size = _StringLength(val) + 2;
    var result = _HeapAlloc(size);
    var result = @kind & @(size - 2) & val;

    return result;
}

fn createExpression(a, op, b) {

}

fn main() {
}