#include "/libs/incl/libload.eon"
#include "/libs/incl/sys.eon"

fn main() {
    setupLibLoad();
    loadLib("/libs/eon.ell");

    var file_name = getArg(1);
    var output_name = "out.eep";

    if (getArg(2) != "") {
        outName = getArg(2);
    }

    _eonFile(file_name, output_name, 0);
    
    return 0;
}
